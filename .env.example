DOMIX_SERVER_PORT=8080
AUTH_SERVER_PORT=5000
NGINX_PORT=80

# Google Identity Services client id, baked into the domix-client build (Vite
# env vars are compile-time). Must match GOOGLE_CLIENT_ID in auth-server/.env.
# Leave empty to build without the "Continue with Google" button.
VITE_GOOGLE_CLIENT_ID=